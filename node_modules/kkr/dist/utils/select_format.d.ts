import { Track } from "../core/mpd_parser";
export interface Tracks {
    videoTracks: Track[];
    audioTracks: Track[];
}
declare const selectFormat: (format: string, tracks: Tracks) => {
    selectedVideoTrack: Track;
    selectedAudioTrack: Track;
};
export default selectFormat;
