/// <reference types="node" />
import { EventEmitter } from "events";
/**
 * 命令执行
 * @param {string} command
 */
export default class CommandExecuter extends EventEmitter {
    run(command: any, { output }?: {
        output?: string[];
    }): void;
}
